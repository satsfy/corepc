// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `31` - hidden.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v31::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports, unused_variables)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::bip32::{self, Xpriv, Xpub};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::hex::FromHex as _;
use bitcoin::key::{self, PrivateKey, PublicKey};
use bitcoin::{
    amount, block, hex, network, psbt, sign_message, witness_program, witness_version, Address,
    Amount, Block, BlockHash, CompactTarget, FeeRate, Network, OutPoint, Psbt, ScriptBuf, Sequence,
    SignedAmount, Target, Transaction, TxMerkleNode, TxOut, Txid, Weight, WitnessProgram,
    WitnessVersion, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v31::generated::compatibility;

impl EstimateRawFee {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::EstimateRawFee, EstimateRawFeeError> {
        use EstimateRawFeeError as E;

        Ok(model::EstimateRawFee {
            short: self.short.map(|x| x.into_model()).transpose().map_err(E::Short)?,
            medium: self.medium.map(|x| x.into_model()).transpose().map_err(E::Medium)?,
            long: self.long.into_model().map_err(E::Long)?,
        })
    }
}

/// Error when converting a `EstimateRawFee` type into the model type.
#[derive(Debug)]
pub enum EstimateRawFeeError {
    /// Conversion of the `Short` field failed.
    Short(RawFeeDetailError),
    /// Conversion of the `Medium` field failed.
    Medium(RawFeeDetailError),
    /// Conversion of the `Long` field failed.
    Long(RawFeeDetailError),
}

impl fmt::Display for EstimateRawFeeError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Short(ref e) => write_err!(f, "conversion of the `Short` field failed"; e),
            Self::Medium(ref e) => write_err!(f, "conversion of the `Medium` field failed"; e),
            Self::Long(ref e) => write_err!(f, "conversion of the `Long` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for EstimateRawFeeError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Short(ref e) => Some(e),
            Self::Medium(ref e) => Some(e),
            Self::Long(ref e) => Some(e),
        }
    }
}

impl EstimateRawFeeLong {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::RawFeeDetail, RawFeeDetailError> {
        use RawFeeDetailError as E;

        Ok(model::RawFeeDetail {
            fee_rate: None, // no raw field; canonical is optional
            // TODO(codegen): canonical field `decay: f64` has no raw counterpart; needs manual reconstruction.
            decay: todo!("no raw field for `decay`"),
            // TODO(codegen): canonical field `scale: u32` has no raw counterpart; needs manual reconstruction.
            scale: todo!("no raw field for `scale`"),
            pass: None,   // no raw field; canonical is optional
            fail: None,   // no raw field; canonical is optional
            errors: None, // no raw field; canonical is optional
        })
    }
}

impl EstimateRawFeeMedium {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::RawFeeDetail, RawFeeDetailError> {
        use RawFeeDetailError as E;

        Ok(model::RawFeeDetail {
            fee_rate: None, // no raw field; canonical is optional
            // TODO(codegen): canonical field `decay: f64` has no raw counterpart; needs manual reconstruction.
            decay: todo!("no raw field for `decay`"),
            // TODO(codegen): canonical field `scale: u32` has no raw counterpart; needs manual reconstruction.
            scale: todo!("no raw field for `scale`"),
            pass: None,   // no raw field; canonical is optional
            fail: None,   // no raw field; canonical is optional
            errors: None, // no raw field; canonical is optional
        })
    }
}

impl EstimateRawFeeShort {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::RawFeeDetail, RawFeeDetailError> {
        use RawFeeDetailError as E;

        Ok(model::RawFeeDetail {
            fee_rate: self
                .fee_rate
                .map(|f| crate::btc_per_kb(f).map_err(E::FeeRate))
                .transpose()?
                .flatten(),
            decay: self.decay,
            scale: crate::to_u32(self.scale, "scale")?,
            pass: self.pass.map(|x| x.into_model()).transpose().map_err(E::Pass)?,
            fail: self.fail.map(|x| x.into_model()).transpose().map_err(E::Fail)?,
            errors: self.errors,
        })
    }
}

/// Error when converting a `RawFeeDetail` type into the model type.
#[derive(Debug)]
pub enum RawFeeDetailError {
    /// Conversion of the `FeeRate` field failed.
    FeeRate(amount::ParseAmountError),
    /// Conversion of the `Pass` field failed.
    Pass(RawFeeRangeError),
    /// Conversion of the `Fail` field failed.
    Fail(RawFeeRangeError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for RawFeeDetailError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::FeeRate(ref e) => write_err!(f, "conversion of the `FeeRate` field failed"; e),
            Self::Pass(ref e) => write_err!(f, "conversion of the `Pass` field failed"; e),
            Self::Fail(ref e) => write_err!(f, "conversion of the `Fail` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for RawFeeDetailError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::FeeRate(ref e) => Some(e),
            Self::Pass(ref e) => Some(e),
            Self::Fail(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for RawFeeDetailError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl EstimateRawFeeShortFail {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::RawFeeRange, RawFeeRangeError> {
        use RawFeeRangeError as E;

        Ok(model::RawFeeRange {
            start_range: crate::btc_per_kb(self.startrange).ok().flatten(),
            end_range: crate::btc_per_kb(self.endrange).ok().flatten(),
            // TODO(codegen): canonical field `within_target: f64` has no raw counterpart; needs manual reconstruction.
            within_target: todo!("no raw field for `within_target`"),
            // TODO(codegen): canonical field `total_confirmed: f64` has no raw counterpart; needs manual reconstruction.
            total_confirmed: todo!("no raw field for `total_confirmed`"),
            // TODO(codegen): canonical field `in_mempool: f64` has no raw counterpart; needs manual reconstruction.
            in_mempool: todo!("no raw field for `in_mempool`"),
            // TODO(codegen): canonical field `left_mempool: f64` has no raw counterpart; needs manual reconstruction.
            left_mempool: todo!("no raw field for `left_mempool`"),
        })
    }
}

impl EstimateRawFeeShortPass {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::RawFeeRange, RawFeeRangeError> {
        use RawFeeRangeError as E;

        Ok(model::RawFeeRange {
            start_range: crate::btc_per_kb(self.startrange).ok().flatten(),
            end_range: crate::btc_per_kb(self.endrange).ok().flatten(),
            within_target: self.within_target,
            total_confirmed: self.total_confirmed,
            in_mempool: self.in_mempool,
            left_mempool: self.left_mempool,
        })
    }
}

/// Error when converting a `RawFeeRange` type into the model type.
#[derive(Debug)]
pub enum RawFeeRangeError {}

impl fmt::Display for RawFeeRangeError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for RawFeeRangeError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}

impl Generate {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::Generate, GenerateError> {
        use GenerateError as E;

        // TODO(codegen): reconstruct `Vec<BlockHash>` from the decoded fields of `Generate`.
        // The canonical `Generate` is a newtype around `Vec<BlockHash>`, but the raw response is the
        // already-decoded value with no single field to convert. Needs manual assembly.
        Ok(model::Generate(todo!("reconstruct Vec<BlockHash> from decoded fields")))
    }
}

/// Error when converting a `Generate` type into the model type.
#[derive(Debug)]
pub enum GenerateError {}

impl fmt::Display for GenerateError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for GenerateError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}

impl GenerateBlock {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GenerateBlock, GenerateBlockError> {
        use GenerateBlockError as E;

        Ok(model::GenerateBlock {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            hex: self
                .hex
                .map(|x| encode::deserialize_hex::<Block>(&x))
                .transpose()
                .map_err(E::Hex)?,
        })
    }
}

/// Error when converting a `GenerateBlock` type into the model type.
#[derive(Debug)]
pub enum GenerateBlockError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of the `Hex` field failed.
    Hex(encode::FromHexError),
}

impl fmt::Display for GenerateBlockError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::Hex(ref e) => write_err!(f, "conversion of the `Hex` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GenerateBlockError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::Hex(ref e) => Some(e),
        }
    }
}

impl GenerateToAddress {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GenerateToAddress, GenerateToAddressError> {
        use GenerateToAddressError as E;

        Ok(model::GenerateToAddress(
            self.0
                .into_iter()
                .map(|x| x.parse::<BlockHash>().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GenerateToAddress` type into the model type.
#[derive(Debug)]
pub enum GenerateToAddressError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GenerateToAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GenerateToAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GenerateToDescriptor {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GenerateToDescriptor, GenerateToDescriptorError> {
        use GenerateToDescriptorError as E;

        Ok(model::GenerateToDescriptor(
            self.0
                .into_iter()
                .map(|x| x.parse::<BlockHash>().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GenerateToDescriptor` type into the model type.
#[derive(Debug)]
pub enum GenerateToDescriptorError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GenerateToDescriptorError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GenerateToDescriptorError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetMempoolFeeRateDiagram {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetMempoolFeerateDiagram, GetMempoolFeerateDiagramError> {
        use GetMempoolFeerateDiagramError as E;

        Ok(model::GetMempoolFeerateDiagram(
            self.0
                .into_iter()
                .map(|x| x.into_model().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GetMempoolFeerateDiagram` type into the model type.
#[derive(Debug)]
pub enum GetMempoolFeerateDiagramError {
    /// Conversion of the `Inner` field failed.
    Inner(FeerateDiagramEntryError),
}

impl fmt::Display for GetMempoolFeerateDiagramError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolFeerateDiagramError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetMempoolFeeRateDiagramItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::FeerateDiagramEntry, FeerateDiagramEntryError> {
        use FeerateDiagramEntryError as E;

        Ok(model::FeerateDiagramEntry {
            weight: crate::to_u64(self.weight, "weight")?,
            fee: Amount::from_btc(self.fee).map_err(E::Fee)?,
        })
    }
}

/// Error when converting a `FeerateDiagramEntry` type into the model type.
#[derive(Debug)]
pub enum FeerateDiagramEntryError {
    /// Conversion of the `Fee` field failed.
    Fee(amount::ParseAmountError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for FeerateDiagramEntryError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Fee(ref e) => write_err!(f, "conversion of the `Fee` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for FeerateDiagramEntryError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Fee(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for FeerateDiagramEntryError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetOrphanTxsVerbose0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetOrphanTxs, GetOrphanTxsError> {
        use GetOrphanTxsError as E;

        Ok(model::GetOrphanTxs(
            self.0
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GetOrphanTxs` type into the model type.
#[derive(Debug)]
pub enum GetOrphanTxsError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GetOrphanTxsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetOrphanTxsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetOrphanTxsVerbose1 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetOrphanTxsVerboseOne, GetOrphanTxsVerboseOneError> {
        use GetOrphanTxsVerboseOneError as E;

        Ok(model::GetOrphanTxsVerboseOne(
            self.0
                .into_iter()
                .map(|x| x.into_model().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GetOrphanTxsVerboseOne` type into the model type.
#[derive(Debug)]
pub enum GetOrphanTxsVerboseOneError {
    /// Conversion of the `Inner` field failed.
    Inner(GetOrphanTxsVerboseOneEntryError),
}

impl fmt::Display for GetOrphanTxsVerboseOneError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetOrphanTxsVerboseOneError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetOrphanTxsVerbose1Item {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetOrphanTxsVerboseOneEntry, GetOrphanTxsVerboseOneEntryError> {
        use GetOrphanTxsVerboseOneEntryError as E;

        Ok(model::GetOrphanTxsVerboseOneEntry {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            wtxid: self.wtxid.parse::<Wtxid>().map_err(E::Wtxid)?,
            bytes: self.bytes,
            vsize: crate::to_u64(self.vsize, "vsize")?,
            weight: crate::to_u64(self.weight, "weight")?,
            entry_time: None,      // no raw field; canonical is optional
            expiration_time: None, // no raw field; canonical is optional
            from: self
                .from
                .into_iter()
                .map(|x| crate::to_u64(x, "from"))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `GetOrphanTxsVerboseOneEntry` type into the model type.
#[derive(Debug)]
pub enum GetOrphanTxsVerboseOneEntryError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `Wtxid` field failed.
    Wtxid(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetOrphanTxsVerboseOneEntryError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::Wtxid(ref e) => write_err!(f, "conversion of the `Wtxid` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetOrphanTxsVerboseOneEntryError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::Wtxid(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetOrphanTxsVerboseOneEntryError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetOrphanTxsVerbose2 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetOrphanTxsVerboseTwo, GetOrphanTxsVerboseTwoError> {
        use GetOrphanTxsVerboseTwoError as E;

        Ok(model::GetOrphanTxsVerboseTwo(
            self.0
                .into_iter()
                .map(|x| x.into_model().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GetOrphanTxsVerboseTwo` type into the model type.
#[derive(Debug)]
pub enum GetOrphanTxsVerboseTwoError {
    /// Conversion of the `Inner` field failed.
    Inner(GetOrphanTxsVerboseTwoEntryError),
}

impl fmt::Display for GetOrphanTxsVerboseTwoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetOrphanTxsVerboseTwoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetOrphanTxsVerbose2Item {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetOrphanTxsVerboseTwoEntry, GetOrphanTxsVerboseTwoEntryError> {
        use GetOrphanTxsVerboseTwoEntryError as E;

        Ok(model::GetOrphanTxsVerboseTwoEntry {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            wtxid: self.wtxid.parse::<Wtxid>().map_err(E::Wtxid)?,
            bytes: self.bytes,
            vsize: crate::to_u64(self.vsize, "vsize")?,
            weight: crate::to_u64(self.weight, "weight")?,
            entry_time: None,      // no raw field; canonical is optional
            expiration_time: None, // no raw field; canonical is optional
            from: self
                .from
                .into_iter()
                .map(|x| crate::to_u64(x, "from"))
                .collect::<Result<Vec<_>, _>>()?,
            transaction: encode::deserialize_hex::<Transaction>(&self.hex)
                .map_err(E::Transaction)?,
        })
    }
}

/// Error when converting a `GetOrphanTxsVerboseTwoEntry` type into the model type.
#[derive(Debug)]
pub enum GetOrphanTxsVerboseTwoEntryError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `Wtxid` field failed.
    Wtxid(hex::HexToArrayError),
    /// Conversion of the `Transaction` field failed.
    Transaction(encode::FromHexError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetOrphanTxsVerboseTwoEntryError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::Wtxid(ref e) => write_err!(f, "conversion of the `Wtxid` field failed"; e),
            Self::Transaction(ref e) => {
                write_err!(f, "conversion of the `Transaction` field failed"; e)
            }
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetOrphanTxsVerboseTwoEntryError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::Wtxid(ref e) => Some(e),
            Self::Transaction(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetOrphanTxsVerboseTwoEntryError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}
