// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `31` - util.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v31::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports, unused_variables)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::bip32::{self, Xpriv, Xpub};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::hex::FromHex as _;
use bitcoin::key::{self, PrivateKey, PublicKey};
use bitcoin::{
    amount, block, hex, network, psbt, sign_message, witness_program, witness_version, Address,
    Amount, Block, BlockHash, CompactTarget, FeeRate, Network, OutPoint, Psbt, ScriptBuf, Sequence,
    SignedAmount, Target, Transaction, TxMerkleNode, TxOut, Txid, Weight, WitnessProgram,
    WitnessVersion, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v31::generated::compatibility;

impl CreateMultisig {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::CreateMultisig, CreateMultisigError> {
        use CreateMultisigError as E;

        Ok(model::CreateMultisig {
            address: self.address.parse::<Address<NetworkUnchecked>>().map_err(E::Address)?,
            redeem_script: ScriptBuf::from_hex(&self.redeem_script).map_err(E::RedeemScript)?,
            descriptor: Some(self.descriptor),
            warnings: self.warnings,
        })
    }
}

/// Error when converting a `CreateMultisig` type into the model type.
#[derive(Debug)]
pub enum CreateMultisigError {
    /// Conversion of the `Address` field failed.
    Address(address::ParseError),
    /// Conversion of the `RedeemScript` field failed.
    RedeemScript(hex::HexToBytesError),
}

impl fmt::Display for CreateMultisigError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Address(ref e) => write_err!(f, "conversion of the `Address` field failed"; e),
            Self::RedeemScript(ref e) =>
                write_err!(f, "conversion of the `RedeemScript` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for CreateMultisigError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Address(ref e) => Some(e),
            Self::RedeemScript(ref e) => Some(e),
        }
    }
}

impl EstimateSmartFee {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::EstimateSmartFee, EstimateSmartFeeError> {
        use EstimateSmartFeeError as E;

        Ok(model::EstimateSmartFee {
            fee_rate: self
                .fee_rate
                .map(|f| crate::btc_per_kb(f).map_err(E::FeeRate))
                .transpose()?
                .flatten(),
            errors: self.errors,
            blocks: crate::to_u32(self.blocks, "blocks")?,
        })
    }
}

/// Error when converting a `EstimateSmartFee` type into the model type.
#[derive(Debug)]
pub enum EstimateSmartFeeError {
    /// Conversion of the `FeeRate` field failed.
    FeeRate(amount::ParseAmountError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for EstimateSmartFeeError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::FeeRate(ref e) => write_err!(f, "conversion of the `FeeRate` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for EstimateSmartFeeError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::FeeRate(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for EstimateSmartFeeError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl SignMessageWithPrivKey {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SignMessageWithPrivKey, SignMessageWithPrivKeyError> {
        use SignMessageWithPrivKeyError as E;

        Ok(model::SignMessageWithPrivKey(
            self.0.parse::<sign_message::MessageSignature>().map_err(E::Inner)?,
        ))
    }
}

/// Error when converting a `SignMessageWithPrivKey` type into the model type.
#[derive(Debug)]
pub enum SignMessageWithPrivKeyError {
    /// Conversion of the `Inner` field failed.
    Inner(sign_message::MessageSignatureError),
}

impl fmt::Display for SignMessageWithPrivKeyError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SignMessageWithPrivKeyError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl ValidateAddress {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::ValidateAddress, ValidateAddressError> {
        use ValidateAddressError as E;

        Ok(model::ValidateAddress {
            is_valid: self.isvalid,
            address: self
                .address
                .map(|x| x.parse::<Address<NetworkUnchecked>>())
                .transpose()
                .map_err(E::Address)?
                .ok_or(E::AddressMissing(crate::MissingField { field: "address" }))?,
            script_pubkey: self
                .script_pub_key
                .map(|x| ScriptBuf::from_hex(&x))
                .transpose()
                .map_err(E::ScriptPubkey)?
                .ok_or(E::ScriptPubkeyMissing(crate::MissingField { field: "script_pubkey" }))?,
            is_script: self.is_script.unwrap_or_default(),
            is_witness: self.is_witness.unwrap_or_default(),
            witness_version: match (self.witness_version, self.witness_program.as_ref()) {
                (Some(v), Some(_)) => Some(
                    WitnessVersion::try_from(u8::try_from(v).map_err(|_| {
                        crate::NumericError::Overflow {
                            value: v,
                            field: "witness_version".to_owned(),
                        }
                    })?)
                    .map_err(E::WitnessVersion)?,
                ),
                _ => None,
            },
            witness_program: match (self.witness_version, self.witness_program.as_ref()) {
                (Some(v), Some(p)) => Some({
                    let wv = WitnessVersion::try_from(u8::try_from(v).map_err(|_| {
                        crate::NumericError::Overflow {
                            value: v,
                            field: "witness_program".to_owned(),
                        }
                    })?)
                    .map_err(E::WitnessVersion)?;
                    let bytes = Vec::<u8>::from_hex(p).map_err(E::WitnessProgramBytes)?;
                    WitnessProgram::new(wv, &bytes).map_err(E::WitnessProgram)?
                }),
                _ => None,
            },
        })
    }
}

/// Error when converting a `ValidateAddress` type into the model type.
#[derive(Debug)]
pub enum ValidateAddressError {
    /// Conversion of the `AddressMissing` field failed.
    AddressMissing(crate::MissingField),
    /// Conversion of the `Address` field failed.
    Address(address::ParseError),
    /// Conversion of the `ScriptPubkeyMissing` field failed.
    ScriptPubkeyMissing(crate::MissingField),
    /// Conversion of the `ScriptPubkey` field failed.
    ScriptPubkey(hex::HexToBytesError),
    /// Conversion of the `WitnessVersion` field failed.
    WitnessVersion(witness_version::TryFromError),
    /// Conversion of the `WitnessProgramBytes` field failed.
    WitnessProgramBytes(hex::HexToBytesError),
    /// Conversion of the `WitnessProgram` field failed.
    WitnessProgram(witness_program::Error),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for ValidateAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::AddressMissing(ref e) =>
                write_err!(f, "conversion of the `AddressMissing` field failed"; e),
            Self::Address(ref e) => write_err!(f, "conversion of the `Address` field failed"; e),
            Self::ScriptPubkeyMissing(ref e) =>
                write_err!(f, "conversion of the `ScriptPubkeyMissing` field failed"; e),
            Self::ScriptPubkey(ref e) =>
                write_err!(f, "conversion of the `ScriptPubkey` field failed"; e),
            Self::WitnessVersion(ref e) =>
                write_err!(f, "conversion of the `WitnessVersion` field failed"; e),
            Self::WitnessProgramBytes(ref e) =>
                write_err!(f, "conversion of the `WitnessProgramBytes` field failed"; e),
            Self::WitnessProgram(ref e) =>
                write_err!(f, "conversion of the `WitnessProgram` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ValidateAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::AddressMissing(ref e) => Some(e),
            Self::Address(ref e) => Some(e),
            Self::ScriptPubkeyMissing(ref e) => Some(e),
            Self::ScriptPubkey(ref e) => Some(e),
            Self::WitnessVersion(ref e) => Some(e),
            Self::WitnessProgramBytes(ref e) => Some(e),
            Self::WitnessProgram(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for ValidateAddressError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}
