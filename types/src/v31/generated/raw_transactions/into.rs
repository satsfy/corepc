// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `31` - rawtransactions.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v31::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports, unused_variables)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::bip32::{self, Xpriv, Xpub};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::hex::FromHex as _;
use bitcoin::key::{self, PrivateKey, PublicKey};
use bitcoin::{
    amount, block, hex, network, psbt, sign_message, witness_program, witness_version, Address,
    Amount, Block, BlockHash, CompactTarget, FeeRate, Network, OutPoint, Psbt, ScriptBuf, Sequence,
    SignedAmount, Target, Transaction, TxMerkleNode, TxOut, Txid, Weight, WitnessProgram,
    WitnessVersion, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v31::generated::compatibility;

impl AbortPrivateBroadcast {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::AbortPrivateBroadcast, AbortPrivateBroadcastError> {
        use AbortPrivateBroadcastError as E;

        Ok(model::AbortPrivateBroadcast {
            removed_transactions: self
                .removed_transactions
                .into_iter()
                .map(|x| crate::reconstruct::transaction(&x).map_err(E::RemovedTransactions))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `AbortPrivateBroadcast` type into the model type.
#[derive(Debug)]
pub enum AbortPrivateBroadcastError {
    /// Conversion of the `RemovedTransactions` field failed.
    RemovedTransactions(crate::reconstruct::ReconstructError),
}

impl fmt::Display for AbortPrivateBroadcastError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::RemovedTransactions(ref e) =>
                write_err!(f, "conversion of the `RemovedTransactions` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for AbortPrivateBroadcastError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::RemovedTransactions(ref e) => Some(e),
        }
    }
}

impl AnalyzePsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::AnalyzePsbt, AnalyzePsbtError> {
        use AnalyzePsbtError as E;

        Ok(model::AnalyzePsbt {
            inputs: self
                .inputs
                .unwrap_or_default()
                .into_iter()
                .map(|x| x.into_model().map_err(E::Inputs))
                .collect::<Result<Vec<_>, _>>()?,
            estimated_vsize: self
                .estimated_vsize
                .map(|x| {
                    u32::try_from(x).map_err(|_| crate::NumericError::Overflow {
                        value: x as i64,
                        field: "estimated_vsize".to_owned(),
                    })
                })
                .transpose()?,
            estimated_fee_rate: self
                .estimated_fee_rate
                .map(|f| crate::btc_per_kb(f).map_err(E::EstimatedFeeRate))
                .transpose()?
                .flatten(),
            fee: self.fee.map(Amount::from_btc).transpose().map_err(E::Fee)?,
            next: self.next,
        })
    }
}

/// Error when converting a `AnalyzePsbt` type into the model type.
#[derive(Debug)]
pub enum AnalyzePsbtError {
    /// Conversion of the `Inputs` field failed.
    Inputs(AnalyzePsbtInputError),
    /// Conversion of the `EstimatedFeeRate` field failed.
    EstimatedFeeRate(amount::ParseAmountError),
    /// Conversion of the `Fee` field failed.
    Fee(amount::ParseAmountError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for AnalyzePsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inputs(ref e) => write_err!(f, "conversion of the `Inputs` field failed"; e),
            Self::EstimatedFeeRate(ref e) =>
                write_err!(f, "conversion of the `EstimatedFeeRate` field failed"; e),
            Self::Fee(ref e) => write_err!(f, "conversion of the `Fee` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for AnalyzePsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inputs(ref e) => Some(e),
            Self::EstimatedFeeRate(ref e) => Some(e),
            Self::Fee(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for AnalyzePsbtError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl AnalyzePsbtInputsItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::AnalyzePsbtInput, AnalyzePsbtInputError> {
        use AnalyzePsbtInputError as E;

        Ok(model::AnalyzePsbtInput {
            has_utxo: self.has_utxo,
            is_final: self.is_final,
            missing: self.missing.map(|x| x.into_model()).transpose().map_err(E::Missing)?,
            next: self.next,
        })
    }
}

/// Error when converting a `AnalyzePsbtInput` type into the model type.
#[derive(Debug)]
pub enum AnalyzePsbtInputError {
    /// Conversion of the `Missing` field failed.
    Missing(AnalyzePsbtInputMissingError),
}

impl fmt::Display for AnalyzePsbtInputError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Missing(ref e) => write_err!(f, "conversion of the `Missing` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for AnalyzePsbtInputError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Missing(ref e) => Some(e),
        }
    }
}

impl AnalyzePsbtInputsItemMissing {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::AnalyzePsbtInputMissing, AnalyzePsbtInputMissingError> {
        use AnalyzePsbtInputMissingError as E;

        Ok(model::AnalyzePsbtInputMissing {
            pubkeys: self
                .pubkeys
                .unwrap_or_default()
                .into_iter()
                .map(|x| x.parse::<hash160::Hash>().map_err(E::Pubkeys))
                .collect::<Result<Vec<_>, _>>()?,
            signatures: self
                .signatures
                .unwrap_or_default()
                .into_iter()
                .map(|x| x.parse::<hash160::Hash>().map_err(E::Signatures))
                .collect::<Result<Vec<_>, _>>()?,
            redeem_script: self
                .redeem_script
                .map(|x| x.parse::<hash160::Hash>())
                .transpose()
                .map_err(E::RedeemScript)?,
            witness_script: self
                .witness_script
                .map(|x| x.parse::<sha256::Hash>())
                .transpose()
                .map_err(E::WitnessScript)?,
        })
    }
}

/// Error when converting a `AnalyzePsbtInputMissing` type into the model type.
#[derive(Debug)]
pub enum AnalyzePsbtInputMissingError {
    /// Conversion of the `Pubkeys` field failed.
    Pubkeys(hex::HexToArrayError),
    /// Conversion of the `Signatures` field failed.
    Signatures(hex::HexToArrayError),
    /// Conversion of the `RedeemScript` field failed.
    RedeemScript(hex::HexToArrayError),
    /// Conversion of the `WitnessScript` field failed.
    WitnessScript(hex::HexToArrayError),
}

impl fmt::Display for AnalyzePsbtInputMissingError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Pubkeys(ref e) => write_err!(f, "conversion of the `Pubkeys` field failed"; e),
            Self::Signatures(ref e) =>
                write_err!(f, "conversion of the `Signatures` field failed"; e),
            Self::RedeemScript(ref e) =>
                write_err!(f, "conversion of the `RedeemScript` field failed"; e),
            Self::WitnessScript(ref e) =>
                write_err!(f, "conversion of the `WitnessScript` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for AnalyzePsbtInputMissingError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Pubkeys(ref e) => Some(e),
            Self::Signatures(ref e) => Some(e),
            Self::RedeemScript(ref e) => Some(e),
            Self::WitnessScript(ref e) => Some(e),
        }
    }
}

impl CombinePsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::CombinePsbt, CombinePsbtError> {
        use CombinePsbtError as E;

        Ok(model::CombinePsbt(self.0.parse::<Psbt>().map_err(E::Inner)?))
    }
}

/// Error when converting a `CombinePsbt` type into the model type.
#[derive(Debug)]
pub enum CombinePsbtError {
    /// Conversion of the `Inner` field failed.
    Inner(psbt::PsbtParseError),
}

impl fmt::Display for CombinePsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for CombinePsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl CombineRawTransaction {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::CombineRawTransaction, CombineRawTransactionError> {
        use CombineRawTransactionError as E;

        Ok(model::CombineRawTransaction(
            encode::deserialize_hex::<Transaction>(&self.0).map_err(E::Inner)?,
        ))
    }
}

/// Error when converting a `CombineRawTransaction` type into the model type.
#[derive(Debug)]
pub enum CombineRawTransactionError {
    /// Conversion of the `Inner` field failed.
    Inner(encode::FromHexError),
}

impl fmt::Display for CombineRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for CombineRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl ConvertToPsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::ConvertToPsbt, ConvertToPsbtError> {
        use ConvertToPsbtError as E;

        Ok(model::ConvertToPsbt(self.0.parse::<Psbt>().map_err(E::Inner)?))
    }
}

/// Error when converting a `ConvertToPsbt` type into the model type.
#[derive(Debug)]
pub enum ConvertToPsbtError {
    /// Conversion of the `Inner` field failed.
    Inner(psbt::PsbtParseError),
}

impl fmt::Display for ConvertToPsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ConvertToPsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl CreatePsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::CreatePsbt, CreatePsbtError> {
        use CreatePsbtError as E;

        Ok(model::CreatePsbt(self.0.parse::<Psbt>().map_err(E::Inner)?))
    }
}

/// Error when converting a `CreatePsbt` type into the model type.
#[derive(Debug)]
pub enum CreatePsbtError {
    /// Conversion of the `Inner` field failed.
    Inner(psbt::PsbtParseError),
}

impl fmt::Display for CreatePsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for CreatePsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl CreateRawTransaction {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::CreateRawTransaction, CreateRawTransactionError> {
        use CreateRawTransactionError as E;

        Ok(model::CreateRawTransaction(
            encode::deserialize_hex::<Transaction>(&self.0).map_err(E::Inner)?,
        ))
    }
}

/// Error when converting a `CreateRawTransaction` type into the model type.
#[derive(Debug)]
pub enum CreateRawTransactionError {
    /// Conversion of the `Inner` field failed.
    Inner(encode::FromHexError),
}

impl fmt::Display for CreateRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for CreateRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl DecodePsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::DecodePsbt, DecodePsbtError> {
        use DecodePsbtError as E;

        Ok(model::DecodePsbt {
            psbt: crate::reconstruct::psbt(&self).map_err(E::Psbt)?,
            fee: self.fee.map(Amount::from_btc).transpose().map_err(E::Fee)?,
        })
    }
}

/// Error when converting a `DecodePsbt` type into the model type.
#[derive(Debug)]
pub enum DecodePsbtError {
    /// Conversion of the `Psbt` field failed.
    Psbt(crate::reconstruct::ReconstructPsbtError),
    /// Conversion of the `Fee` field failed.
    Fee(amount::ParseAmountError),
}

impl fmt::Display for DecodePsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Psbt(ref e) => write_err!(f, "conversion of the `Psbt` field failed"; e),
            Self::Fee(ref e) => write_err!(f, "conversion of the `Fee` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for DecodePsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Psbt(ref e) => Some(e),
            Self::Fee(ref e) => Some(e),
        }
    }
}

impl DecodeRawTransaction {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::DecodeRawTransaction, DecodeRawTransactionError> {
        use DecodeRawTransactionError as E;

        Ok(model::DecodeRawTransaction(
            crate::reconstruct::transaction(&self).map_err(E::Reconstruct)?,
        ))
    }
}

/// Error when converting a `DecodeRawTransaction` type into the model type.
#[derive(Debug)]
pub enum DecodeRawTransactionError {
    /// Conversion of the `Reconstruct` field failed.
    Reconstruct(crate::reconstruct::ReconstructError),
}

impl fmt::Display for DecodeRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Reconstruct(ref e) =>
                write_err!(f, "conversion of the `Reconstruct` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for DecodeRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Reconstruct(ref e) => Some(e),
        }
    }
}

impl DecodeScript {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::DecodeScript, DecodeScriptError> {
        use DecodeScriptError as E;

        Ok(model::DecodeScript {
            script_pubkey: None, // no raw field; canonical is optional
            descriptor: Some(self.desc),
            type_: self.type_,
            address: self
                .address
                .map(|x| x.parse::<Address<NetworkUnchecked>>())
                .transpose()
                .map_err(E::Address)?,
            required_signatures: None, // no raw field; canonical is optional
            addresses: vec![],         // no raw field; canonical is a list
            p2sh: self
                .p2sh
                .map(|x| x.parse::<Address<NetworkUnchecked>>())
                .transpose()
                .map_err(E::P2sh)?,
            p2sh_segwit: None, // no raw field; canonical is optional
        })
    }
}

/// Error when converting a `DecodeScript` type into the model type.
#[derive(Debug)]
pub enum DecodeScriptError {
    /// Conversion of the `Address` field failed.
    Address(address::ParseError),
    /// Conversion of the `P2sh` field failed.
    P2sh(address::ParseError),
}

impl fmt::Display for DecodeScriptError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Address(ref e) => write_err!(f, "conversion of the `Address` field failed"; e),
            Self::P2sh(ref e) => write_err!(f, "conversion of the `P2sh` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for DecodeScriptError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Address(ref e) => Some(e),
            Self::P2sh(ref e) => Some(e),
        }
    }
}

impl DescriptorProcessPsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::DescriptorProcessPsbt, DescriptorProcessPsbtError> {
        use DescriptorProcessPsbtError as E;

        Ok(model::DescriptorProcessPsbt {
            psbt: self.psbt.parse::<Psbt>().map_err(E::Psbt)?,
            complete: self.complete,
            tx: self
                .hex
                .map(|x| encode::deserialize_hex::<Transaction>(&x))
                .transpose()
                .map_err(E::Tx)?,
        })
    }
}

/// Error when converting a `DescriptorProcessPsbt` type into the model type.
#[derive(Debug)]
pub enum DescriptorProcessPsbtError {
    /// Conversion of the `Psbt` field failed.
    Psbt(psbt::PsbtParseError),
    /// Conversion of the `Tx` field failed.
    Tx(encode::FromHexError),
}

impl fmt::Display for DescriptorProcessPsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Psbt(ref e) => write_err!(f, "conversion of the `Psbt` field failed"; e),
            Self::Tx(ref e) => write_err!(f, "conversion of the `Tx` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for DescriptorProcessPsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Psbt(ref e) => Some(e),
            Self::Tx(ref e) => Some(e),
        }
    }
}

impl FinalizePsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::FinalizePsbt, FinalizePsbtError> {
        use FinalizePsbtError as E;

        Ok(model::FinalizePsbt {
            psbt: self.psbt.map(|x| x.parse::<Psbt>()).transpose().map_err(E::Psbt)?,
            tx: self
                .hex
                .map(|x| encode::deserialize_hex::<Transaction>(&x))
                .transpose()
                .map_err(E::Tx)?,
            complete: self.complete,
        })
    }
}

/// Error when converting a `FinalizePsbt` type into the model type.
#[derive(Debug)]
pub enum FinalizePsbtError {
    /// Conversion of the `Psbt` field failed.
    Psbt(psbt::PsbtParseError),
    /// Conversion of the `Tx` field failed.
    Tx(encode::FromHexError),
}

impl fmt::Display for FinalizePsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Psbt(ref e) => write_err!(f, "conversion of the `Psbt` field failed"; e),
            Self::Tx(ref e) => write_err!(f, "conversion of the `Tx` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for FinalizePsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Psbt(ref e) => Some(e),
            Self::Tx(ref e) => Some(e),
        }
    }
}

impl FundRawTransaction {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::FundRawTransaction, FundRawTransactionError> {
        use FundRawTransactionError as E;

        Ok(model::FundRawTransaction {
            tx: encode::deserialize_hex::<Transaction>(&self.hex).map_err(E::Tx)?,
            fee: Amount::from_btc(self.fee).map_err(E::Fee)?,
            change_position: self.changepos,
        })
    }
}

/// Error when converting a `FundRawTransaction` type into the model type.
#[derive(Debug)]
pub enum FundRawTransactionError {
    /// Conversion of the `Tx` field failed.
    Tx(encode::FromHexError),
    /// Conversion of the `Fee` field failed.
    Fee(amount::ParseAmountError),
}

impl fmt::Display for FundRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Tx(ref e) => write_err!(f, "conversion of the `Tx` field failed"; e),
            Self::Fee(ref e) => write_err!(f, "conversion of the `Fee` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for FundRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Tx(ref e) => Some(e),
            Self::Fee(ref e) => Some(e),
        }
    }
}

impl GetPrivateBroadcastInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetPrivateBroadcastInfo, GetPrivateBroadcastInfoError> {
        use GetPrivateBroadcastInfoError as E;

        Ok(model::GetPrivateBroadcastInfo {
            transactions: self
                .transactions
                .into_iter()
                .map(|x| crate::reconstruct::transaction(&x).map_err(E::Transactions))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `GetPrivateBroadcastInfo` type into the model type.
#[derive(Debug)]
pub enum GetPrivateBroadcastInfoError {
    /// Conversion of the `Transactions` field failed.
    Transactions(crate::reconstruct::ReconstructError),
}

impl fmt::Display for GetPrivateBroadcastInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Transactions(ref e) =>
                write_err!(f, "conversion of the `Transactions` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetPrivateBroadcastInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Transactions(ref e) => Some(e),
        }
    }
}

impl GetRawTransactionVerbose0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetRawTransaction, GetRawTransactionError> {
        use GetRawTransactionError as E;

        Ok(model::GetRawTransaction(
            encode::deserialize_hex::<Transaction>(&self.0).map_err(E::Inner)?,
        ))
    }
}

/// Error when converting a `GetRawTransaction` type into the model type.
#[derive(Debug)]
pub enum GetRawTransactionError {
    /// Conversion of the `Inner` field failed.
    Inner(encode::FromHexError),
}

impl fmt::Display for GetRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetRawTransactionVerbose1 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetRawTransactionVerbose, GetRawTransactionVerboseError> {
        use GetRawTransactionVerboseError as E;

        Ok(model::GetRawTransactionVerbose {
            in_active_chain: self.in_active_chain,
            transaction: encode::deserialize_hex::<Transaction>(&self.hex)
                .map_err(E::Transaction)?,
            block_hash: self
                .block_hash
                .map(|x| x.parse::<BlockHash>())
                .transpose()
                .map_err(E::BlockHash)?,
            confirmations: self
                .confirmations
                .map(|x| crate::to_u64(x, "confirmations"))
                .transpose()?,
            transaction_time: self
                .time
                .map(|x| crate::to_u64(x, "transaction_time"))
                .transpose()?,
            block_time: self.block_time.map(|x| crate::to_u64(x, "block_time")).transpose()?,
        })
    }
}

/// Error when converting a `GetRawTransactionVerbose` type into the model type.
#[derive(Debug)]
pub enum GetRawTransactionVerboseError {
    /// Conversion of the `Transaction` field failed.
    Transaction(encode::FromHexError),
    /// Conversion of the `BlockHash` field failed.
    BlockHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetRawTransactionVerboseError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Transaction(ref e) =>
                write_err!(f, "conversion of the `Transaction` field failed"; e),
            Self::BlockHash(ref e) =>
                write_err!(f, "conversion of the `BlockHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetRawTransactionVerboseError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Transaction(ref e) => Some(e),
            Self::BlockHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetRawTransactionVerboseError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl JoinPsbts {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::JoinPsbts, JoinPsbtsError> {
        use JoinPsbtsError as E;

        Ok(model::JoinPsbts(self.0.parse::<Psbt>().map_err(E::Inner)?))
    }
}

/// Error when converting a `JoinPsbts` type into the model type.
#[derive(Debug)]
pub enum JoinPsbtsError {
    /// Conversion of the `Inner` field failed.
    Inner(psbt::PsbtParseError),
}

impl fmt::Display for JoinPsbtsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for JoinPsbtsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl SendRawTransaction {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SendRawTransaction, SendRawTransactionError> {
        use SendRawTransactionError as E;

        Ok(model::SendRawTransaction(self.0.parse::<Txid>().map_err(E::Inner)?))
    }
}

/// Error when converting a `SendRawTransaction` type into the model type.
#[derive(Debug)]
pub enum SendRawTransactionError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for SendRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SendRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl SignRawTransactionWithKey {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SignRawTransaction, SignRawTransactionError> {
        use SignRawTransactionError as E;

        Ok(model::SignRawTransaction {
            tx: encode::deserialize_hex::<Transaction>(&self.hex).map_err(E::Tx)?,
            complete: self.complete,
            errors: self
                .errors
                .unwrap_or_default()
                .into_iter()
                .map(|x| x.into_model().map_err(E::Errors))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `SignRawTransaction` type into the model type.
#[derive(Debug)]
pub enum SignRawTransactionError {
    /// Conversion of the `Tx` field failed.
    Tx(encode::FromHexError),
    /// Conversion of the `Errors` field failed.
    Errors(SignFailError),
}

impl fmt::Display for SignRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Tx(ref e) => write_err!(f, "conversion of the `Tx` field failed"; e),
            Self::Errors(ref e) => write_err!(f, "conversion of the `Errors` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SignRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Tx(ref e) => Some(e),
            Self::Errors(ref e) => Some(e),
        }
    }
}

impl SignRawTransactionWithKeyErrorsItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SignFail, SignFailError> {
        use SignFailError as E;

        Ok(model::SignFail {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            vout: crate::to_u64(self.vout, "vout")?,
            script_sig: ScriptBuf::from_hex(&self.script_sig).map_err(E::ScriptSig)?,
            sequence: Sequence::from_consensus(self.sequence as u32),
            error: self.error,
        })
    }
}

/// Error when converting a `SignFail` type into the model type.
#[derive(Debug)]
pub enum SignFailError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `ScriptSig` field failed.
    ScriptSig(hex::HexToBytesError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for SignFailError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::ScriptSig(ref e) =>
                write_err!(f, "conversion of the `ScriptSig` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SignFailError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::ScriptSig(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for SignFailError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl SubmitPackage {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SubmitPackage, SubmitPackageError> {
        use SubmitPackageError as E;

        Ok(model::SubmitPackage {
            package_msg: self.package_msg,
            tx_results: self
                .tx_results
                .into_iter()
                .map(|(k, v)| {
                    Ok::<_, E>((
                        k.parse::<Wtxid>().map_err(E::TxResultsKey)?,
                        v.into_model().map_err(E::TxResultsValue)?,
                    ))
                })
                .collect::<Result<std::collections::BTreeMap<_, _>, _>>()?,
            replaced_transactions: self
                .replaced_transactions
                .unwrap_or_default()
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::ReplacedTransactions))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `SubmitPackage` type into the model type.
#[derive(Debug)]
pub enum SubmitPackageError {
    /// Conversion of the `TxResultsKey` field failed.
    TxResultsKey(hex::HexToArrayError),
    /// Conversion of the `TxResultsValue` field failed.
    TxResultsValue(SubmitPackageTxResultError),
    /// Conversion of the `ReplacedTransactions` field failed.
    ReplacedTransactions(hex::HexToArrayError),
}

impl fmt::Display for SubmitPackageError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::TxResultsKey(ref e) =>
                write_err!(f, "conversion of the `TxResultsKey` field failed"; e),
            Self::TxResultsValue(ref e) =>
                write_err!(f, "conversion of the `TxResultsValue` field failed"; e),
            Self::ReplacedTransactions(ref e) =>
                write_err!(f, "conversion of the `ReplacedTransactions` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SubmitPackageError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::TxResultsKey(ref e) => Some(e),
            Self::TxResultsValue(ref e) => Some(e),
            Self::ReplacedTransactions(ref e) => Some(e),
        }
    }
}

impl SubmitPackageTxResults {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SubmitPackageTxResult, SubmitPackageTxResultError> {
        use SubmitPackageTxResultError as E;

        Ok(model::SubmitPackageTxResult {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            other_wtxid: self
                .other_wtxid
                .map(|x| x.parse::<Wtxid>())
                .transpose()
                .map_err(E::OtherWtxid)?,
            vsize: self.vsize.map(|x| crate::to_u32(x, "vsize")).transpose()?,
            fees: self.fees.map(|x| x.into_model()).transpose().map_err(E::Fees)?,
            error: self.error,
        })
    }
}

/// Error when converting a `SubmitPackageTxResult` type into the model type.
#[derive(Debug)]
pub enum SubmitPackageTxResultError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `OtherWtxid` field failed.
    OtherWtxid(hex::HexToArrayError),
    /// Conversion of the `Fees` field failed.
    Fees(SubmitPackageTxResultFeesError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for SubmitPackageTxResultError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::OtherWtxid(ref e) =>
                write_err!(f, "conversion of the `OtherWtxid` field failed"; e),
            Self::Fees(ref e) => write_err!(f, "conversion of the `Fees` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SubmitPackageTxResultError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::OtherWtxid(ref e) => Some(e),
            Self::Fees(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for SubmitPackageTxResultError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl SubmitPackageTxResultsFees {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::SubmitPackageTxResultFees, SubmitPackageTxResultFeesError> {
        use SubmitPackageTxResultFeesError as E;

        Ok(model::SubmitPackageTxResultFees {
            base_fee: Amount::from_btc(self.base).map_err(E::BaseFee)?,
            effective_fee_rate: self
                .effective_fee_rate
                .map(|f| crate::btc_per_kb(f).map_err(E::EffectiveFeeRate))
                .transpose()?
                .flatten(),
            effective_includes: self
                .effective_includes
                .unwrap_or_default()
                .into_iter()
                .map(|x| x.parse::<Wtxid>().map_err(E::EffectiveIncludes))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `SubmitPackageTxResultFees` type into the model type.
#[derive(Debug)]
pub enum SubmitPackageTxResultFeesError {
    /// Conversion of the `BaseFee` field failed.
    BaseFee(amount::ParseAmountError),
    /// Conversion of the `EffectiveFeeRate` field failed.
    EffectiveFeeRate(amount::ParseAmountError),
    /// Conversion of the `EffectiveIncludes` field failed.
    EffectiveIncludes(hex::HexToArrayError),
}

impl fmt::Display for SubmitPackageTxResultFeesError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::BaseFee(ref e) => write_err!(f, "conversion of the `BaseFee` field failed"; e),
            Self::EffectiveFeeRate(ref e) =>
                write_err!(f, "conversion of the `EffectiveFeeRate` field failed"; e),
            Self::EffectiveIncludes(ref e) =>
                write_err!(f, "conversion of the `EffectiveIncludes` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SubmitPackageTxResultFeesError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::BaseFee(ref e) => Some(e),
            Self::EffectiveFeeRate(ref e) => Some(e),
            Self::EffectiveIncludes(ref e) => Some(e),
        }
    }
}

impl TestMempoolAccept {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::TestMempoolAccept, TestMempoolAcceptError> {
        use TestMempoolAcceptError as E;

        Ok(model::TestMempoolAccept {
            results: self
                .0
                .into_iter()
                .map(|x| x.into_model().map_err(E::Results))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `TestMempoolAccept` type into the model type.
#[derive(Debug)]
pub enum TestMempoolAcceptError {
    /// Conversion of the `Results` field failed.
    Results(MempoolAcceptanceError),
}

impl fmt::Display for TestMempoolAcceptError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Results(ref e) => write_err!(f, "conversion of the `Results` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for TestMempoolAcceptError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Results(ref e) => Some(e),
        }
    }
}

impl TestMempoolAcceptItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolAcceptance, MempoolAcceptanceError> {
        use MempoolAcceptanceError as E;

        Ok(model::MempoolAcceptance {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            wtxid: Some(self.wtxid.parse::<Wtxid>().map_err(E::Wtxid)?),
            allowed: self.allowed.unwrap_or_default(),
            vsize: self.vsize.map(|x| crate::to_u32(x, "vsize")).transpose()?,
            fees: self.fees.map(|x| x.into_model()).transpose().map_err(E::Fees)?,
            reject_reason: self.reject_reason,
            reject_details: self.reject_details,
        })
    }
}

/// Error when converting a `MempoolAcceptance` type into the model type.
#[derive(Debug)]
pub enum MempoolAcceptanceError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `Wtxid` field failed.
    Wtxid(hex::HexToArrayError),
    /// Conversion of the `Fees` field failed.
    Fees(MempoolAcceptanceFeesError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for MempoolAcceptanceError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::Wtxid(ref e) => write_err!(f, "conversion of the `Wtxid` field failed"; e),
            Self::Fees(ref e) => write_err!(f, "conversion of the `Fees` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for MempoolAcceptanceError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::Wtxid(ref e) => Some(e),
            Self::Fees(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for MempoolAcceptanceError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl TestMempoolAcceptItemFees {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolAcceptanceFees, MempoolAcceptanceFeesError> {
        use MempoolAcceptanceFeesError as E;

        Ok(model::MempoolAcceptanceFees {
            base: Amount::from_btc(self.base).map_err(E::Base)?,
            effective_fee_rate: crate::btc_per_kb(self.effective_fee_rate)
                .map_err(E::EffectiveFeeRate)?,
            effective_includes: Some(
                self.effective_includes
                    .into_iter()
                    .map(|x| x.parse::<Wtxid>().map_err(E::EffectiveIncludes))
                    .collect::<Result<Vec<_>, _>>()?,
            ),
        })
    }
}

/// Error when converting a `MempoolAcceptanceFees` type into the model type.
#[derive(Debug)]
pub enum MempoolAcceptanceFeesError {
    /// Conversion of the `Base` field failed.
    Base(amount::ParseAmountError),
    /// Conversion of the `EffectiveFeeRate` field failed.
    EffectiveFeeRate(amount::ParseAmountError),
    /// Conversion of the `EffectiveIncludes` field failed.
    EffectiveIncludes(hex::HexToArrayError),
}

impl fmt::Display for MempoolAcceptanceFeesError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Base(ref e) => write_err!(f, "conversion of the `Base` field failed"; e),
            Self::EffectiveFeeRate(ref e) =>
                write_err!(f, "conversion of the `EffectiveFeeRate` field failed"; e),
            Self::EffectiveIncludes(ref e) =>
                write_err!(f, "conversion of the `EffectiveIncludes` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for MempoolAcceptanceFeesError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Base(ref e) => Some(e),
            Self::EffectiveFeeRate(ref e) => Some(e),
            Self::EffectiveIncludes(ref e) => Some(e),
        }
    }
}

impl UtxoUpdatePsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::UtxoUpdatePsbt, UtxoUpdatePsbtError> {
        use UtxoUpdatePsbtError as E;

        Ok(model::UtxoUpdatePsbt(self.0.parse::<Psbt>().map_err(E::Inner)?))
    }
}

/// Error when converting a `UtxoUpdatePsbt` type into the model type.
#[derive(Debug)]
pub enum UtxoUpdatePsbtError {
    /// Conversion of the `Inner` field failed.
    Inner(psbt::PsbtParseError),
}

impl fmt::Display for UtxoUpdatePsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for UtxoUpdatePsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}
