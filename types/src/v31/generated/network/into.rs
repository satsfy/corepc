// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `31` - network.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v31::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports, unused_variables)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::bip32::{self, Xpriv, Xpub};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::hex::FromHex as _;
use bitcoin::key::{self, PrivateKey, PublicKey};
use bitcoin::{
    amount, block, hex, network, psbt, sign_message, witness_program, witness_version, Address,
    Amount, Block, BlockHash, CompactTarget, FeeRate, Network, OutPoint, Psbt, ScriptBuf, Sequence,
    SignedAmount, Target, Transaction, TxMerkleNode, TxOut, Txid, Weight, WitnessProgram,
    WitnessVersion, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v31::generated::compatibility;

impl GetNetworkInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetNetworkInfo, GetNetworkInfoError> {
        use GetNetworkInfoError as E;

        Ok(model::GetNetworkInfo {
            version: usize::try_from(self.version).map_err(|_| crate::NumericError::Overflow {
                value: self.version,
                field: "version".to_owned(),
            })?,
            subversion: self.sub_version,
            protocol_version: usize::try_from(self.protocol_version).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.protocol_version,
                    field: "protocol_version".to_owned(),
                }
            })?,
            local_services: self.localservices,
            local_services_names: Some(self.localservices_names),
            local_relay: self.local_relay,
            time_offset: isize::try_from(self.time_offset).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.time_offset,
                    field: "time_offset".to_owned(),
                }
            })?,
            connections: usize::try_from(self.connections).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.connections as i64,
                    field: "connections".to_owned(),
                }
            })?,
            connections_in: Some(usize::try_from(self.connections_in).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.connections_in as i64,
                    field: "connections_in".to_owned(),
                }
            })?),
            connections_out: Some(usize::try_from(self.connections_out).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.connections_out as i64,
                    field: "connections_out".to_owned(),
                }
            })?),
            network_active: self.network_active,
            networks: self
                .networks
                .into_iter()
                .map(|x| x.into_model().map_err(E::Networks))
                .collect::<Result<Vec<_>, _>>()?,
            relay_fee: crate::btc_per_kb(self.relay_fee).map_err(E::RelayFee)?,
            incremental_fee: crate::btc_per_kb(self.incremental_fee).map_err(E::IncrementalFee)?,
            local_addresses: self
                .local_addresses
                .into_iter()
                .map(|x| x.into_model().map_err(E::LocalAddresses))
                .collect::<Result<Vec<_>, _>>()?,
            warnings: self.warnings,
        })
    }
}

/// Error when converting a `GetNetworkInfo` type into the model type.
#[derive(Debug)]
pub enum GetNetworkInfoError {
    /// Conversion of the `Networks` field failed.
    Networks(GetNetworkInfoNetworkError),
    /// Conversion of the `RelayFee` field failed.
    RelayFee(amount::ParseAmountError),
    /// Conversion of the `IncrementalFee` field failed.
    IncrementalFee(amount::ParseAmountError),
    /// Conversion of the `LocalAddresses` field failed.
    LocalAddresses(GetNetworkInfoAddressError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetNetworkInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Networks(ref e) => write_err!(f, "conversion of the `Networks` field failed"; e),
            Self::RelayFee(ref e) => write_err!(f, "conversion of the `RelayFee` field failed"; e),
            Self::IncrementalFee(ref e) =>
                write_err!(f, "conversion of the `IncrementalFee` field failed"; e),
            Self::LocalAddresses(ref e) =>
                write_err!(f, "conversion of the `LocalAddresses` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetNetworkInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Networks(ref e) => Some(e),
            Self::RelayFee(ref e) => Some(e),
            Self::IncrementalFee(ref e) => Some(e),
            Self::LocalAddresses(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetNetworkInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetNetworkInfoLocalAddressesItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetNetworkInfoAddress, GetNetworkInfoAddressError> {
        use GetNetworkInfoAddressError as E;

        Ok(model::GetNetworkInfoAddress {
            address: self.address,
            port: u16::try_from(self.port).map_err(|_| crate::NumericError::Overflow {
                value: self.port,
                field: "port".to_owned(),
            })?,
            score: crate::to_u32(self.score, "score")?,
        })
    }
}

/// Error when converting a `GetNetworkInfoAddress` type into the model type.
#[derive(Debug)]
pub enum GetNetworkInfoAddressError {
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetNetworkInfoAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetNetworkInfoAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetNetworkInfoAddressError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetNetworkInfoNetworksItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetNetworkInfoNetwork, GetNetworkInfoNetworkError> {
        use GetNetworkInfoNetworkError as E;

        Ok(model::GetNetworkInfoNetwork {
            name: self.name,
            limited: self.limited,
            reachable: self.reachable,
            proxy: self.proxy,
            proxy_randomize_credentials: self.proxy_randomize_credentials,
        })
    }
}

/// Error when converting a `GetNetworkInfoNetwork` type into the model type.
#[derive(Debug)]
pub enum GetNetworkInfoNetworkError {}

impl fmt::Display for GetNetworkInfoNetworkError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for GetNetworkInfoNetworkError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}
