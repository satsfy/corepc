// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `31` - mining.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v31::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports, unused_variables)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::bip32::{self, Xpriv, Xpub};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::hex::FromHex as _;
use bitcoin::key::{self, PrivateKey, PublicKey};
use bitcoin::{
    amount, block, hex, network, psbt, sign_message, witness_program, witness_version, Address,
    Amount, Block, BlockHash, CompactTarget, FeeRate, Network, OutPoint, Psbt, ScriptBuf, Sequence,
    SignedAmount, Target, Transaction, TxMerkleNode, TxOut, Txid, Weight, WitnessProgram,
    WitnessVersion, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v31::generated::compatibility;

impl GetMiningInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetMiningInfo, GetMiningInfoError> {
        use GetMiningInfoError as E;

        Ok(model::GetMiningInfo {
            blocks: crate::to_u64(self.blocks, "blocks")?,
            current_block_weight: self.current_block_weight.map(|x| Weight::from_wu(x as u64)),
            current_block_tx: self.current_block_tx,
            bits: Some(CompactTarget::from_unprefixed_hex(&self.bits).map_err(E::Bits)?),
            difficulty: self.difficulty,
            target: Some(Target::from_unprefixed_hex(&self.target).map_err(E::Target)?),
            network_hash_ps: self.network_hash_ps,
            pooled_tx: i64::try_from(self.pooled_tx).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.pooled_tx as i64,
                    field: "pooled_tx".to_owned(),
                }
            })?,
            block_min_tx_fee: crate::btc_per_kb(self.block_min_tx_fee).map_err(E::BlockMinTxFee)?,
            chain: self.chain,
            signet_challenge: self.signet_challenge,
            next: Some(self.next.into_model().map_err(E::Next)?),
            warnings: self.warnings,
        })
    }
}

/// Error when converting a `GetMiningInfo` type into the model type.
#[derive(Debug)]
pub enum GetMiningInfoError {
    /// Conversion of the `Bits` field failed.
    Bits(UnprefixedHexError),
    /// Conversion of the `Target` field failed.
    Target(UnprefixedHexError),
    /// Conversion of the `BlockMinTxFee` field failed.
    BlockMinTxFee(amount::ParseAmountError),
    /// Conversion of the `Next` field failed.
    Next(NextBlockInfoError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetMiningInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Bits(ref e) => write_err!(f, "conversion of the `Bits` field failed"; e),
            Self::Target(ref e) => write_err!(f, "conversion of the `Target` field failed"; e),
            Self::BlockMinTxFee(ref e) =>
                write_err!(f, "conversion of the `BlockMinTxFee` field failed"; e),
            Self::Next(ref e) => write_err!(f, "conversion of the `Next` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMiningInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Bits(ref e) => Some(e),
            Self::Target(ref e) => Some(e),
            Self::BlockMinTxFee(ref e) => Some(e),
            Self::Next(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetMiningInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetMiningInfoNext {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::NextBlockInfo, NextBlockInfoError> {
        use NextBlockInfoError as E;

        Ok(model::NextBlockInfo {
            height: crate::to_u64(self.height, "height")?,
            bits: CompactTarget::from_unprefixed_hex(&self.bits).map_err(E::Bits)?,
            difficulty: self.difficulty,
            target: Target::from_unprefixed_hex(&self.target).map_err(E::Target)?,
        })
    }
}

/// Error when converting a `NextBlockInfo` type into the model type.
#[derive(Debug)]
pub enum NextBlockInfoError {
    /// Conversion of the `Bits` field failed.
    Bits(UnprefixedHexError),
    /// Conversion of the `Target` field failed.
    Target(UnprefixedHexError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for NextBlockInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Bits(ref e) => write_err!(f, "conversion of the `Bits` field failed"; e),
            Self::Target(ref e) => write_err!(f, "conversion of the `Target` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for NextBlockInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Bits(ref e) => Some(e),
            Self::Target(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for NextBlockInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetPrioritisedTransactions {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetPrioritisedTransactions, GetPrioritisedTransactionsError> {
        use GetPrioritisedTransactionsError as E;

        Ok(model::GetPrioritisedTransactions(
            self.0
                .into_iter()
                .map(|(k, v)| {
                    Ok::<_, E>((
                        k.parse::<Txid>().map_err(E::InnerKey)?,
                        v.into_model().map_err(E::InnerValue)?,
                    ))
                })
                .collect::<Result<std::collections::BTreeMap<_, _>, _>>()?,
        ))
    }
}

/// Error when converting a `GetPrioritisedTransactions` type into the model type.
#[derive(Debug)]
pub enum GetPrioritisedTransactionsError {
    /// Conversion of the `InnerKey` field failed.
    InnerKey(hex::HexToArrayError),
    /// Conversion of the `InnerValue` field failed.
    InnerValue(PrioritisedTransactionError),
}

impl fmt::Display for GetPrioritisedTransactionsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::InnerKey(ref e) => write_err!(f, "conversion of the `InnerKey` field failed"; e),
            Self::InnerValue(ref e) =>
                write_err!(f, "conversion of the `InnerValue` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetPrioritisedTransactionsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::InnerKey(ref e) => Some(e),
            Self::InnerValue(ref e) => Some(e),
        }
    }
}

impl GetPrioritisedTransactionsEntry {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::PrioritisedTransaction, PrioritisedTransactionError> {
        use PrioritisedTransactionError as E;

        Ok(model::PrioritisedTransaction {
            fee_delta: SignedAmount::from_sat(self.fee_delta),
            in_mempool: self.in_mempool,
            modified_fee: self.modified_fee.map(SignedAmount::from_sat),
        })
    }
}

/// Error when converting a `PrioritisedTransaction` type into the model type.
#[derive(Debug)]
pub enum PrioritisedTransactionError {}

impl fmt::Display for PrioritisedTransactionError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for PrioritisedTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}
